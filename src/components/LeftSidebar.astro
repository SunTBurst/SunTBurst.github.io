---
import CalendarWidget from './CalendarWidget.svelte';
import SidebarTags from './SidebarTags.astro';

interface CalendarPost {
  slug: string;
  title: string;
  date: string;
  description: string;
}

export interface Props {
  posts?: CalendarPost[];
  tags: { name: string; count: number }[];
  activeTag?: string | null;
  showCalendar?: boolean;
  showTags?: boolean;
}

const { posts = [], tags, activeTag = null, showCalendar = true, showTags = true } = Astro.props;
---

{
  showCalendar && <CalendarWidget posts={posts} client:idle />
}
{
  showTags && <SidebarTags tags={tags} activeTag={activeTag} />
}
