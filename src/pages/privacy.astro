---
import Layout from '../layouts/Layout.astro';
import { commentPublicConfig } from '../features/comments/config';

const providerLabels = { openai: 'OpenAI', kimi: 'Kimi', deepseek: 'DeepSeek' } as const;
const commentsEnabled = commentPublicConfig.state === 'enabled';
const commentProvider = commentPublicConfig.reviewProvider ? providerLabels[commentPublicConfig.reviewProvider] : null;
---

<Layout title="隐私说明" description="本站的隐私与数据使用说明">
  <article class="prose prose-lg mx-auto max-w-3xl border-4 border-[#0284c7] bg-white p-6 shadow-[8px_8px_0_0_#0284c7] dark:bg-slate-800 sm:p-10">
    <h1>隐私说明</h1>
    <p>本站以静态个人博客为主体，不接入访客分析和广告服务。评论模块已完成代码准备，但只有在本页明确显示“已启用”时才会提供 GitHub 登录和评论输入。</p>
    <h2>GitHub 评论与审核</h2>
    {commentsEnabled ? (
      <p>评论服务当前已启用。登录由 GitHub 和 Supabase Auth 处理；本站会保存 GitHub 公开登录名、数字身份标识、评论正文、目标页面、时间和审核记录。评论在公开前由 {commentProvider} 审核，不能明确通过时转人工审核。拒绝公开正文最长保留 30 天，等待人工审核正文最长保留 90 天，操作记录最长保留 180 天；作者或管理员删除时正文会被清空。详情见<a href="/comments-policy">评论与审核规则</a>。</p>
    ) : (
      <p>评论服务当前处于预览状态。页面不显示登录和输入框，不下载 Supabase 评论客户端，不连接评论端点，也不会把 GitHub 身份、评论文字或页面信息发送给 Supabase、OpenAI、Kimi 或 DeepSeek。公开规则页仅说明未来启用条件。</p>
    )}
    <h2>天气工具</h2>
    <p>利雅得天气页面只在你主动点击“查看利雅得实时天气”后，向 Open-Meteo 发送一次请求。请求包含固定的利雅得坐标、时区和所需天气字段，不读取你的设备定位，不发送邮箱、搜索词或浏览内容。外部服务可能依其政策处理 IP 地址和常规请求信息；本站不保存该请求或返回结果。</p>
    <h2>本地浏览器数据</h2>
    <p>浏览器可能保存深浅主题偏好。收藏和阅读足迹仅在访客主动使用相应功能时保存在当前浏览器，可在“收藏与足迹”页面清空。</p>
    <h2>随机画片</h2>
    <p>随机画片全部保存在本站 GitHub Pages 静态资源中，切换图片不会请求第三方图片服务。图片的尺寸、替代文本、来源和使用权记录在仓库清单中。</p>
    <h2>本地声音空间</h2>
    <p>声音空间只在你点击开始后，使用浏览器内置能力本地合成声音；不加载音频文件，不发送网络请求，也不写入本地存储。本站不记录播放行为、场景选择、音量或播放时长，离开页面时声音引擎会停止。</p>
    <h2>公开知识问答</h2>
    <p>AI 导览基础版只在当前浏览器中，把你输入的问题与构建时发布的公开索引进行匹配；问题不会离开当前页面，不会写入本地存储，也不调用外部模型。返回内容来自公开标题、简介和主题，并提供可打开的站内来源。</p>
    <h2>友链申请与站外书签</h2>
    <p>友链申请会跳转到本仓库的公开 GitHub Issue 表单，本站不在浏览器内收集申请资料。不要在 Issue 中填写邮箱、手机号或其他非公开个人信息；提交后适用 GitHub 的隐私政策。站外书签只在访客点击后打开对应网站，之后的数据处理由该网站自行说明。</p>
    <h2>今日漫游</h2>
    <p>今日漫游只用利雅得日期和构建时发布的公开候选列表确定三站路线；不读取设备定位，不写入本地存储，也不根据访问历史或个人行为生成画像。同一天的所有访客看到同一条路线。</p>
    <h2>RSS 与 OPML</h2>
    <p>RSS 和 OPML 是公开静态文件，访问、复制或下载时本站不接收邮箱，也不在浏览器内保存订阅行为。你使用的 RSS 阅读器可能依其自身政策处理订阅地址和阅读记录，相关边界应以该阅读器的说明为准。</p>
    <h2>GitHub 公开活动</h2>
    <p>GitHub 活动页的数据由 GitHub Actions 在构建期从 SunTBurst 的公开 API 资料生成。公开页只保留用户名、公开数量、仓库摘要、活动类型、来源链接和时间，不包含邮箱、位置、原始 API 载荷、完整提交内容或令牌。浏览该页不会请求 GitHub API；点击 GitHub 链接后适用 GitHub 的隐私政策。</p>
    <h2>站点数据与构建状态</h2>
    <p>站点数据页只计数已发布的公开索引、内容类型和工具状态；构建状态只记录公开提交标识、构建时间和汇总计数。两个页面都不收集访客 IP、设备、访问量、在线人数或精确阅读路径。</p>
  </article>
</Layout>
